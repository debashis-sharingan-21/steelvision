# Severstal's competition never named its 4 defect classes beyond numeric ids —
# these are exactly the labels Kaggle uses (ImageId_ClassId suffix 1-4), not invented.
CLASS_NAMES = ["defect_1", "defect_2", "defect_3", "defect_4"]

IMAGE_HEIGHT = 256
IMAGE_WIDTH = 1600
