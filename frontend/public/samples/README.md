6 real NEU-DET images (one per defect class), copied from this project's own `data/test/`
split, bundled here so the dashboard's "Try a sample" buttons work without requiring a
visitor to have their own steel strip photo. See `../../../data/README.md` for the
dataset's source and licensing (research/educational use).
